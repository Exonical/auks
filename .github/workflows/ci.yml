name: CI

on:
  push:
  pull_request:

jobs:
  build-slurm-plugin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            autoconf \
            automake \
            libtool \
            pkg-config \
            libkrb5-dev \
            libtirpc-dev \
            flex \
            bison \
            libslurm-dev

      - name: Build
        run: |
          autoreconf -i
          ./configure --with-slurm
          make
          make -C src/plugins/slurm clean
          make -C src/plugins/slurm \
            CFLAGS="-Wall -Wextra -Wformat=2 -Werror"
          rust/golden/regen.sh

  autotools-rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            autoconf \
            automake \
            libtool \
            pkg-config \
            libkrb5-dev \
            libtirpc-dev \
            libclang-dev \
            flex \
            bison \
            libslurm-dev

      - uses: dtolnay/rust-toolchain@stable

      - name: Build and install both Slurm plugins
        run: |
          autoreconf -i
          ./configure --with-slurm --enable-rust
          make -j"$(nproc)"
          make DESTDIR="$PWD/dest" install
          test -f dest/usr/local/lib/slurm/auks_rs.so
          test -f dest/usr/local/lib/slurm/auks.so

  compose-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build test image
        run: docker compose build

      - name: Start test services
        run: docker compose up -d

      - name: Run integration tests
        run: docker compose exec -T auks_client bats /tests/simple.bats

      - name: Run Rust client integration tests
        run: docker compose exec -T slurm bats /tests/client_rust.bats

      - name: Run Slurm integration tests
        run: docker compose exec -T slurm runuser -u user -- bats /tests/slurm.bats

      - name: Stop test services
        if: always()
        run: docker compose down -v

  compose-tests-rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build test image
        run: docker compose build

      - name: Start Rust plugin services
        run: AUKS_SPANK_IMPL=rust docker compose up -d

      - name: Run Rust Slurm integration tests
        run: docker compose exec -T slurm runuser -u user -- bats /tests/slurm_rust.bats

      - name: Stop test services
        if: always()
        run: docker compose down -v

  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Kerberos and bindgen dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libkrb5-dev libclang-dev libslurm-dev

      - uses: dtolnay/rust-toolchain@1.98.1
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: rust

      - name: Check formatting
        working-directory: rust
        run: cargo fmt --check

      - name: Run clippy
        working-directory: rust
        run: cargo clippy --all-targets -- -D warnings

      - name: Run tests
        working-directory: rust
        run: cargo test
