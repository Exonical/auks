[package]
name = "auks-spank"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[lib]
crate-type = ["cdylib"]

[dependencies]
auks-spank-sys = { path = "../auks-spank-sys" }

[lints]
workspace = true
